#include<iostream>
// The header line is importing and header file named iostream which is used to perfom functionallity related to input/output


// int-> integer( int is used so that we are defining that the main ouput/return will be an integer) and main() is an function which is an entry point and from which the real execution starts

int main(){
    std::cout <<"Hello World "<<std::endl;
    // cout is an method of std namespace which is inside iostream
    return 0;
}
